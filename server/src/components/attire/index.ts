import { PlayerEvent } from '@infernus/core'

PlayerEvent.onCommandText('buyattire', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('attire', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('wdzb', ({ next }) => {
  return next()
})
