import { PlayerEvent } from '@infernus/core'

PlayerEvent.onCommandText(['help', 'cmds'], ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('anim', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('mynetstats', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('kill', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText(['skin', 'hf'], ({ next }) => {
  return next()
})

PlayerEvent.onCommandText(['fxq', 'jetpack'], ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('weapon', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('jls', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('pos', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('wudi', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText(['tv', 'spec'], ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('kgobj', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('w', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('ww', ({ next }) => {
  return next()
})
