import { PlayerEvent } from '@infernus/core'

PlayerEvent.onCommandText('sound1', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound2', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound3', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound4', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound5', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound6', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('sound7', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('soundStop', ({ next }) => {
  return next()
})

PlayerEvent.onCommandText('moveMe', ({ next }) => {
  return next()
})
